package com.university.oop.demo.third.creational.builder.display;

/**
 * A simple display interface that takes encoded content
 * and translates them to visual images.
 */
public interface Display {
    void displayContent(String someEncodedContent);
}
